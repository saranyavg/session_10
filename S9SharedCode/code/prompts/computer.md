The Computer skill interacts with native desktop applications using cua-driver.
It accepts goal metadata (e.g. "Run task") and app metadata, runs a 5-layer cascade (hotkey -> ax -> cdp -> vision fallback), executes the tasks, and outputs trajectories.
